# cargo-audit configuration for Crush
# See: https://rustsec.org/

[advisories]
# Ignored advisories — transitive dependencies we cannot control
ignore = [
    # paste 1.0.15 (unmaintained) — transitive dep via wgpu 28 → wgpu-hal → metal.
    # We cannot remove it; paste is pulled in by the metal crate (macOS backend).
    # Remove this exemption when wgpu upgrades to a metal version that drops paste.
    "RUSTSEC-2024-0436",
]
