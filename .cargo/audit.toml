# cargo-audit configuration for Crush
# See: https://rustsec.org/

[advisories]
# No ignored advisories.
#
# RUSTSEC-2024-0436 (paste 1.0.15, unmaintained) was exempted while wgpu 28
# reached it via wgpu-hal -> metal. wgpu 30 moved the macOS backend to
# objc2-metal, which does not depend on paste, so the exemption is gone.
# Verified with `cargo tree --target aarch64-apple-darwin` and
# `--target x86_64-apple-darwin`: no paste on any target.
ignore = []
